<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage   xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:ViewModels="clr-namespace:AgentVI.ViewModels;assembly=AgentVI"
                xmlns:local="clr-namespace:AgentVI.Views;assembly=AgentVI"
                xmlns:converters="clr-namespace:AgentVI.Converters;assembly=AgentVI"
                x:Class="AgentVI.Views.FilterPage"
                ItemsSource="{Binding FilteringPagesContent}"
                x:Name="MyFilterPage">
    
   <CarouselPage.Resources>
        <ResourceDictionary>
            <converters:FilterPageIDConverter x:Key="FilterPageIDConverter"/>
        </ResourceDictionary>
    </CarouselPage.Resources>

    <CarouselPage.ItemTemplate>
        <DataTemplate>
            <ContentPage>
                <ContentPage.Padding>
                    <OnPlatform x:TypeArguments="Thickness"
                    iOS="0,30,0,0">
                    </OnPlatform>
                </ContentPage.Padding>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="19*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackLayout x:Name="filterDataStackLayout" Grid.Row="0" HorizontalOptions="CenterAndExpand">
                        <SearchBar x:Name="filterSearchBar" Placeholder="Search..." HeightRequest="30" TextChanged="filterSearchBar_TextChanged"/>
                        <ListView x:Name="filteredItemsListView" ItemSelected="Handle_FilterListItemSelected" ItemsSource="{Binding FoldersList}" BackgroundColor="Transparent" SeparatorColor="Transparent">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextCell Text="{Binding Name}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                    <StackLayout x:Name="filterNumStackLayout" Orientation="Horizontal" Grid.Row="1" HorizontalOptions="CenterAndExpand">
                        <Label Text="Filter Num. "/>
                        <Label x:Name="filterNumLabel" Text="{Binding FilterID, Converter={StaticResource FilterPageIDConverter}}"/>
                    </StackLayout>
                </Grid>
            </ContentPage>
        </DataTemplate>
    </CarouselPage.ItemTemplate>

</CarouselPage>