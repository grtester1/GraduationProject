<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage   xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:ViewModels="clr-namespace:AgentVI.ViewModels;assembly=AgentVI"
                xmlns:local="clr-namespace:AgentVI.Views;assembly=AgentVI"
                xmlns:settings="clr-namespace:AgentVI.Utils"
                xmlns:converters="clr-namespace:AgentVI.Converters;assembly=AgentVI"
                xmlns:ffsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
                x:Class="AgentVI.Views.FilterPage"
                ItemsSource="{Binding FilteringPagesContent}"
                x:Name="MyFilterPage">
    <CarouselPage.Resources>
        <ResourceDictionary>
            <converters:FilterPageIDConverter x:Key="FilterPageIDConverter"/>
        </ResourceDictionary>
    </CarouselPage.Resources>
    <CarouselPage.ItemTemplate>
        <DataTemplate>
            <ContentPage>
                <ContentPage.Padding>
                    <OnPlatform x:TypeArguments="Thickness"
                        iOS="{x:Static settings:Settings.iOSPlatformPadding}">
                    </OnPlatform>
                </ContentPage.Padding>
                <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <RowDefinition Height="18*"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackLayout Grid.Row="0" Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                            <Image Source="{x:Static settings:Settings.LogoPicturePath}" Aspect="AspectFit"/>
                            <ffsvg:SvgCachedImage Source="{x:Static settings:Settings.DividerIconSVGPath}"/>
                            <Label Text="{Binding AccountName}" FontAttributes="Bold" FontSize="{x:Static settings:Settings.GenericTitleFontSize}" VerticalOptions="CenterAndExpand"/>
                        </StackLayout>
                        <Label Grid.Row="1" Text="/Path/Path/Path/Path" HorizontalOptions="StartAndExpand" FontSize="{x:Static settings:Settings.SensorEventDetailsFontSize}"/>
                    </Grid>
                    <StackLayout x:Name="filterDataStackLayout" Grid.Row="1" HorizontalOptions="CenterAndExpand">
                        <SearchBar x:Name="filterSearchBar" Placeholder="{x:Static settings:Settings.SearchBarPlaceHolderText}" HeightRequest="30" TextChanged="filterSearchBar_TextChanged"/>
                        <ListView x:Name="filteredItemsListView" HasUnevenRows="True" ItemSelected="Handle_FilterListItemSelected" ItemsSource="{Binding FoldersList}" BackgroundColor="Transparent" SeparatorColor="Transparent">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <StackLayout HeightRequest="40" Orientation="Horizontal" Padding="{x:Static settings:Settings.PaddingForViewCellInListView}">
                                            <ffsvg:SvgCachedImage Source="{x:Static settings:Settings.FolderIconSVGPath}"/>
                                            <Label HorizontalOptions="StartAndExpand" VerticalOptions="Center" Text="{Binding Name}"/>
                                            <ffsvg:SvgCachedImage Source="{x:Static settings:Settings.BackButtonSVGPath}"/>
                                        </StackLayout>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                    <Button Grid.Row="2" Text="Apply Selection" VerticalOptions="FillAndExpand" TextColor="GhostWhite" BorderColor="WhiteSmoke" BackgroundColor="LightGreen" Clicked="OnSelectionClickedButton" HorizontalOptions="FillAndExpand"/>
                    <!--
                    ###CurrentLevelInFiltration
                    <StackLayout x:Name="filterNumStackLayout" Grid.Row="2" Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                        <Label Text="Filter Num. "/>
                        <Label x:Name="filterNumLabel" Text="{Binding FilterID, Converter={StaticResource FilterPageIDConverter}}"/>
                    </StackLayout>
                    -->
                </Grid>
            </ContentPage>
        </DataTemplate>
    </CarouselPage.ItemTemplate>
</CarouselPage>